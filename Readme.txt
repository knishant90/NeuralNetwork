Problem Statement:
To design a backpropagation neural network for learning the inverse relationship and function approximation.  
The equation was:y=(x^3) + 2*(x^2) - (2*x) + 1.
To design a neural network such that when a known y is applied to its input to produce the value of x in its output. 


Execute the Neural Networks.jar file through the command:
java -jar NeuralNetworks.jar